# 🧠 AGENT.md — دستور وكيل Sokchad v2.0 (النسخة الكاملة)

> **أنت وكيل تطوير مخصص حصرياً لمشروع Sokchad.** هذا الملف ليس نصائح — هو **عقد ملزم**. أي مخالفة له = فشل المهمة حتى لو بنيت الكود. اقرأه كاملاً قبل أي مهمة، وارجع إليه عند كل قرار.

---

## 1️⃣ الهوية والبيئة

- **المالك**: abdarahim — غير مبرمج، يتحدث العربية، يريد تنفيذاً ذاتياً كاملاً بلا أسئلة كثيرة. سؤالك يجب أن يكون نادراً وذكياً (سؤال واحد حاسم فقط عند غموض حقيقي يغير مسار العمل)
- **المشروع**: سوق إلكتروني للهاتف (Android) لحصر بلد تشاد 🇹🇩
  - **15 مدينة تشادية**، الأسعار بالـ **FCFA** (أرقام صحيحة بلا كسور)
  - **3 لغات**: فرنسي (افتراضي LTR) + عربي (RTL كامل) + إنجليزي
  - **3 طرق دفع**: Airtel Money (Dial *222#)، Moov Money (Dial *155#)، الدفع عند الاستلام
  - **التوصيل المحلي**: دراجة نارية / تاكسي — مجاني أو على المشتري
  - بائعون موثوقون + تقييمات ★ + ضمان استرجاع من البائع
- **التقنية مقفلة** (لا تغيّرها):
  - Expo SDK ~53.0.9 / React Native 0.79.3 / React 19.0.0 / TypeScript
  - Expo Router v5 (الملاحة من مجلد `app/`)
  - AsyncStorage للحفظ المحلي
  - الخلفية: Docker (sokchad-api :8080 عبر PHP، sokchad-db MySQL :3307)
  - المحاكي يرى السيرفر عبر `http://10.0.2.2:8080` (ليس localhost!)
- **ممنوع تغيير إصدارات الحزم**: قبل تثبيت أي expo-* جديد افحص توافق expo-modules-core (سبق أن expo-print@57 كسّر البناء مع SDK 53 — الحل كان التثبيت بإصدار متوافق ~14.0.0)
- **التثبيت دائماً**: `npm install --legacy-peer-deps` (بدونه يفشل)

## 2️⃣ القوانين التصميمية المقفلة (كل واحدة درس من فشل حقيقي مدفوع الثمن)

### 1. السيولة الاستجابية — ممنوع المقاسات الثابتة
- **ممنوع**: `scale()` العام للتنسيق، `wp()/hp()`، نقاط توقف `if (width < X)`، `transform: [{scale}]` للتخطيط، `PixelRatio` للتخطيط
- **الصحيح**: `useWindowDimensions()` + Flexbox + `aspectRatio` + `onLayout` للقياسات الحية
- **درس مسجل**: `Dimensions.get()` على مستوى الوحدة = لقطة مجمّدة وقت الاستيراد → عنصر ينهار "بعض الهواتف فقط" ولا يُصلحه أي تعديل مقاسات. احسب دائماً **في وقت الرسم**
- الشاشات القصيرة = تمرير أكثر، وليس ضغط العناصر

### 2. الصور مربعة 1:1 تملأ الإطار حافة-إلى-حافة
- الرفع مفروض مربع 1:1 ≤5MB (expo-image-manipulator) → **أي إطار صورة منتج غير مربع يقصّ المنتج = فشل**
- المالك قال حرفياً: «المنتج يجب أن يملأ الإطار بالكامل ويظهر بكل تفاصيله… جزء من المنتج مختفي»
- الأماكن المصححة سابقاً (لا ترجعها أبداً): `seller/[id]` كان 0.75، `store-profile` كان 120px، `promoted` كان 0.85، `profile` كان 0.8، hero المنتج كان 0.85، الشريط المموّل كان hp('8%')

### 3. نسبة البطاقة = نسبة صفحة التفاصيل
نفس الصورة في مكانين بنسب مختلفة = قصّان مختلفان = «يبدو معطوباً» حتى لو صحيح تقنياً. قس نسبة صفحة التفاصيل أولاً ثم طبّقها على كل البطاقات.

### 4. RTL بمستوى واحد فقط
- `isAr && { flexDirection: 'row-reverse' }` على **المستوى الأدنى المباشر فقط** — العكس الثلاثي (container + wrapper + children) يعيد LTR
- الأسهم الاتجاهية تنقلب (arrow-back ↔ arrow-forward)، الأرقام/الأسعار/الصور/اللوجو **لا تنقلب**
- العناصر المطلقة (left: 16) تبدّل left/right **يدوياً** — row-reverse لا يحركها
- **ممنوع I18nManager.forceRTL** — يزدوج مع اليدوي ويكسر التخطيط

### 5. الترجمة كائن LocalizedText
```typescript
interface LocalizedText { fr: string; ar: string }
// title: { en, fr, ar } في المنتجات — يُقرأ بـ title[language]
```
- لا حقول nameFr/nameAr منفصلة. الأسماء العلم (Boutique Al Madina) لا تُترجم
- **كل نص جديد = 3 لغات (ar/fr/en) دائماً** حتى لو صغيراً

### 6. الفرنسي افتراضي — لا شاشة اختيار لغة عند الإقلاع
العربية اختيارية من الإعدادات، تتحول **فوراً بلا إعادة تشغيل** (عبر حالة React في AppContext، لا متغير وحدة)، وتنجو من إعادة التشغيل (AsyncStorage)

### 7. البحث + زر الفلتر = وحدة واحدة بلا فجوة
```tsx
<View style={searchWrapper}>  {/* overflow:'hidden', borderRadius, بدون border */}
  <View style={searchBar}>…</View>      {/* borderWidth:0, borderRadius:0 */}
  <Pressable style={searchBtn}>…</Pressable>  {/* borderRadius:0 */}
</View>
```
- الهوامش السالبة **تفشل** في RN flex — الغلاف overflow:hidden هو الحل الوحيد الموثوق
- **تحقق بالبكسل (PIL) وليس بنموذج الرؤية**: نماذج الرؤية تكذب وتقول "GAP: YES" عند حدود الألوان الحادة حتى لو الفجوة 0px — تحليل البكسل هو الحقيقة

### 8. ترتيب بطاقة علي-بابا المقفل (بالترتيب)
شارة tagLabel (ذهبية اختيارية) → العنوان (سطران، `adjustsFontSizeToFit minimumFontScale={0.7}`) → صف «مبيعات | ★تقييم (مراجعات)» (نجمة #FFB400) → 📍الموقع → 🚚 التوصيل المجاني (أخضر اختياري) → **السعر أخيراً** (بنفسجي عريض). حقل فارغ = لا صف (لا صفوف فارغة)

### 9. إيموجي واحد لكل زر — لا تزيده أبداً

## 3️⃣ بروتوكول الإنجاز الإلزامي (لكل مهمة، بلا استثناءات)

```
1. اقرأ الملفات ذات العلاقة أولاً — لا تخمّن أبداً
2. عدّل موضعياً (patch) — لا إعادة كتابة ملف كامل إلا للضرورة القصوى
3. npx tsc --noEmit → لا أخطاء جديدة (توجد 10 أخطاء أساس قديمة معروفة 
   في seller-analytics ×3، seller/[id]، AppContext ×5، index.tsx — لا تعالجها ولا تضف عليها)
4. cd android && ./gradlew assembleRelease → يجب BUILD SUCCESSFUL
5. adb uninstall ثم adb install (ممنوع -r مع extractNativeLibs — يكسر المكتبات الأصلية)
6. 3 دورات إعادة تشغيل كاملة — بعد كل دورة: adb logcat -d | grep -c FATAL يجب = 0
7. لقطة شاشة + فحص بعيني (vision) لكل شاشة معدّلة — تأكد أنك على الشاشة الصحيحة 
   أولاً عبر uiautomator dump (التطبيق يستعيد آخر مسار!)
8. التسليم الكامل: APK + tar.gz مصدر + SHA256SUMS + لقطات إثبات — بلا هذه = لم يُنجز شيء
```

- **البناء ≠ الإنجاز**. الإنجاز = كود + صفر FATAL + إثبات بصري + ملفات مسلّمة
- المحاكي: API 30 ≤720p يعمل بسلاسة (لقطة 500-1300KB)؛ API 33+ بطيء الرسم بسبب SwiftShader — **قيد جهاز المضيف وليس خطأ تطبيق**، لا "تصلح" التطبيق بسببه. لقطة <200KB = الصور لم تُحمّل بعد
- صرف الأذونات المنبثقة قبل أي لقطة (اضغط Allow، تحقق 0 حوارات)
- **الفيديو**: سجّل بعملية خلفية، تفاعلات بأمر منفصل أثناء التسجيل، **فك ترميز كامل إجباري قبل الإرسال** (`ffmpeg -i v.mp4 -vsync 0 -f image2 /tmp/p_%02d.png` ثم عدّ الملفات — مئات = سليم، 1 = تالف لا يُرسل). دمج مقطعين: نظّف كل مقطع بـgenpts أولاً ثم concat بـ-c copy بلا إعادة ترميز بعدها (فخ الـ4 ثوانٍ)
- **أخطاء تاريخية ممنوع تكرارها** (كلها وقعت فعلاً):
  - فلتر UUID في تحميل DB **يمحو منتجات البائع** المضافة محلياً (المعرفات p-timestamp لا تطابق UUID) → `prev.filter(p => !dbIds.has(p.id))` فقط
  - المنتجات تختفي بعد إعادة التشغيل → تخزين فوري في AsyncStorage (`sokchad_products_local` حد 50) + دمج عند الإقلاع + مرجع ref للمزامنة في getProductById
  - «Seller not found» → **بائع احتياطي (fallback)** مبني من بيانات منتجاته في **كل** شاشة (بروفايل، checkout، محادثة، فاتورة، طلب) واستخدم effectiveSeller في كل مكان
  - «Aucun mode de paiement» → طرق الدفع الافتراضية لتشاد (Airtel/Moov/COD) تظهر **دائماً** حتى للبائع الاحتياطي
  - نشر بطيء → **لحظي**: حالة + AsyncStorage أولاً، DB بالخلفية غير الحاجبة
  - شعار/بانر البائع يختفي بعد logout → مفتاح لكل بائع `sokchad_seller_profile_{id}` + الدمج عند تسجيل الدخول (لا استبدال)
  - تنبيه نجاح مزعج بعد حفظ الفاتورة → احذفه، أظهر التنبيه عند الفشل فقط
  - زر النشر مخفي خلف شريط التبويب → paddingBottom = insets.bottom + scale(100) في sell.tsx

## 4️⃣ منهجية العمل (مستوى هندسي — ليس اختيارياً)

- **السبب الجذري قبل الإصلاح**: ممنوع الإصلاحات التخمينية. أعد الإنتاج بمحكم ضيق → اعزل → اقرأ رسائل الخطأ كاملة → أصلح السبب لا العرض → أضف حماية من التكرار. **3 إصلاحات فاشلة = راجع البنية، لا إصلاحاً رابعاً**
- **TDD عند إمكانية الاختبار**: اختبار يفشل أولاً (RED) → الكود الأدنى (GREEN) → تنظيف (REFACTOR). اختبار ينجح من أول مرة = لا يختبر شيئاً
- **تسليم تزايدي**: شريحة عمودية واحدة (DB+API+UI) → اختبار → بناء → commit → التالي. أكثر من 100 سطر بلا اختبار = علامة خطر
- **مراجعة 5 محاور** قبل التسليم: الصحة (حالات حدية وأخطاء) / الوضوح (أسماء وبساطة) / البنية (أنماط المشروع) / الأمان / الأداء — مع أرقام لا عبارات عامة
- **git ذري**: كل شريحة commit واحد واضح `feat:|fix:|refactor:` يشرح **لماذا**، لا خلط إصلاح مع ميزة، لا secrets في أي diff
- **الأمان أولاً**: مدخلات المستخدم عدو (تحقق عند الحدود فقط)، لا secrets في الكود/Git/APK/الردود (اكتب `[REDACTED]` دائماً)، استعلامات معاملة بلا دمج نصي، رفع ملفات محقق النوع والحجم، مفاتيح في .env فقط + .gitignore يغطيه
- **الأداء**: قس أولاً — لا تحسين بلا قياس. FlatList: `initialNumToRender` منخفض + `windowSize` مضبوط + `paddingBottom = tabBarHeight + safeArea` فقط (الحشو الزائد يخفي محتوى). React.memo/useMemo عند الحاجة المثبتة لا في كل مكان. الصور التجريبية محلية بـrequire() (offline فوري) — لكن انتبه: require() يعيد رقماً وليس نصاً URI، استخدم helper `productImgSource()` في كل مستهلك

## 5️⃣ الحظر المطلق (الجرائم الثقيلة)

1. **استرجاع نسخة قديمة كاملة** بلا أمر صريح — النقل يكون **ميزة-بميزة** (type → seeds → UI graft → API) مع أرشفة الحالي أولاً. النسخة الذهبية المعتمدة: `SOKCHAD_GOLDEN_SOURCE_v8.0` (بصمة c7cc41fc...298f393، نسبة الصور 0.64192) — هي قاعدة التطوير
2. **تغيير المقاسات غير المطلوب** — المالك يقارن بما يعرف: «ترتكب أخطاء كثيرة جداً وتقول أنجزت ولكن في النهاية لم تنجز»
3. **تسليم بلا اختبار 3 دورات + لقطة** — «لا لقطة حقيقية = غير مُتحقق»
4. **ادعاء إنجاز زائف** — أخطر خطأ على الإطلاق. الشفافية: ما نجح / ما فشل / ما بقي
5. **تجاهل RTL العربي** — جودة العربية = جودة الفرنسية تماماً
6. **لمس خدمات البوتات** المعزولة (@SOKCHADCOD_bot الدبلجة، @Dubyfy_Bot التحميل) دون أمر — تخصصات معزولة على السيرفر
7. **إيموجيات في واجهة التطبيق** أو زخارف بلا أمر صريح
8. **تجاوز هذا الملف** — اقرأه قبل كل مهمة كبيرة، وطبّق حرفياً
9. **حذف نسخ احتياطية/شهادات/مفاتيح** — الحذف فقط بأمر صريح مع قائمة استثناءات

## 6️⃣ أسلوب الرد مع المالك

- **عربي دائماً** في الشرح، تقني إنجليزي في الكود
- تفصيل بأدلة: جداول للنتائج + MEDIA للملفات واللقطات (كل ملف برابط)
- **شفافية مطلقة**: ما نجح / ما فشل / ما بقي — بلا تجميل
- نفّذ ولا تسأل عندما يكون الطلب واضحاً؛ سؤال واحد حاسم عند غموض حقيقي؛ أعلن افتراضاتك ونفّذ عند الاضطرار
- بعد كل تسليم: اقترح الخطوة التالية — لا تتوقف عند «تم»
- بلا إيموجيات زائدة في الردود — نظيف ومنظم بجداول

## 7️⃣ مرجع سريع — الملفات الحرجة

| الملف | الدور | قاعدة |
|---|---|---|
| `app/(tabs)/index.tsx` | الرئيسية | RECENTLY_ADDED_IMG_RATIO = 0.64192 سطر 66 — لا تغيّر دون أمر |
| `components/ProductCard.tsx` | بطاقة المنتج | التاج التلقائي: rating≥4.5 و soldCount≥100 |
| `components/ProductImage.tsx` | عرض الصورة | مربع دائماً، يملأ الإطار |
| `contexts/AppContext.tsx` | الحالة العامة (~2200 سطر) | النمط الرباعي: interface→state→useCallback→provider value (نسيان خطوة 4 = undefined صامت) |
| `services/mockData.ts` | البيانات والواجهات | بذرة: A54 (214/4.7)، Adidas Choice (940/4.4)، Nike (320/4.6+ضمان7) |
| `services/productService.ts` | طبقة API | createProduct + dbRowToProduct يحملان كل الحقول |
| `app/(tabs)/sell.tsx` | نموذج البيع | ضمان (تفعيل+أيام) + توصيل (مجاني/مدفوع+رسوم) |
| `android/app/build.gradle` | الإصدار | versionCode/versionName — لا تغيير دون أمر |
| `constants/responsive.ts` | جسر التصدير | أي تصدير جديد في ui/responsive يجب إضافته للجسر وإلا استيراد صامت فاشل |

## 8️⃣ الفلسفة النهائية

> المالك يقيسك بالنتائج الملموسة، لا بالوعود. كل رسالة إنجاز بلا ملفات ولقطات = لم تنجز.
> كل شاشة تكيفها يجب أن تجمل على أي هاتف — ليس هاتفك فقط.
> كل سطر تكتبه يخدم سوقاً حقيقياً يستخدمه أشخاص حقيقيون في تشاد.
>
> **عمّق، لا تقف عند أول نجاح سطحي، وتحقق بنفسك قبل أن تحكي.**